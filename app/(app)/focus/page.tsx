export default function FocusPage() {
  return (
    <div className="flex min-h-[70vh] flex-col items-center justify-center gap-2 py-6 text-center">
      <p className="text-sm text-neutral-400 dark:text-neutral-500">
        Your focus session now lives in the widget in the corner — it stays
        with you across every page, so it just opened up for you here too.
      </p>
    </div>
  );
}
